// RU: Базовый service worker для будущего PWA-режима.
// EN: Base service worker for future PWA behavior.

self.addEventListener('install', () => {
  // RU: На этом этапе только базовая регистрация service worker.
  // EN: At this stage only base service worker registration is used.
});

self.addEventListener('fetch', () => {
  // RU: Кеширование добавим позже, когда зафиксируем финальную структуру ресурсов.
  // EN: Caching will be added later after the final resource structure is stabilized.
});
