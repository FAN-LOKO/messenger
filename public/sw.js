// RU: Базовый service worker для будущего PWA-режима.
// EN: Base service worker for future PWA behavior.

self.addEventListener('install', () => {
  // RU: На этом этапе только базовая регистрация.
  // EN: At this stage only basic registration is required.
});

self.addEventListener('fetch', () => {
  // RU: Кеширование добавим позже, когда зафиксируем структуру загрузки.
  // EN: Caching will be added later after the loading structure is finalized.
});
